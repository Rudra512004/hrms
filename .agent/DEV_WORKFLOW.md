# Development Workflow

- Run `docker-compose up` to start backing services if needed.
- Frontend: `cd frontend && npm start` (or `npm run dev`)
- Backend: `cd backend && python manage.py runserver`
- Always apply migrations and install dependencies when pulling new changes.
