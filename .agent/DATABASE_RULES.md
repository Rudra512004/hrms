# Database Rules

- All database schema changes must be done via Django migrations.
- Always use the Django ORM to interact with the database.
- Keep models simple, use managers or services for complex logic.
- Enforce data integrity through constraints and indexes.
