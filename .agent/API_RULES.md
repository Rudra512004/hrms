# API Rules

- Use RESTful principles for endpoint design.
- Always include consistent pagination, filtering, and error handling.
- Use appropriate HTTP methods (GET, POST, PUT, PATCH, DELETE).
- Secure APIs with token-based authentication and strict permission checks.
